<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sts.spektrumnew">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".JobOrderDetailScan" android:label="@string/job_order_detail" />
        <activity android:name=".JobOrderDetail" android:label="@string/job_order_detail">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".JobOrderList"  android:label="@string/job_order_list" ></activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>